#include <iostream>

using namespace std;

typedef struct 
{
	float pi;
	float pf;
	float vi;
	float vf;
} Dados;

int main(){
/*
	float tempo;
	Dados d;
	cin >> d.pi >> d.pf >> d.vi >> d.vf >> tempo;

	if(tempo<1){
		cout << ((d.vi/(1.0/tempo))+d.pi) << " ";
		cout << ((d.vf/(1.0/tempo))+d.pf) << endl;
	}else{
		cout << ((d.vi/tempo)+d.pi) << " ";
		cout << ((d.vf/tempo+d.pf)) << endl;
	}
*/
	return 0;
}
